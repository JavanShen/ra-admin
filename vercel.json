{
    "outputDirectory": "build",
    "devCommand": "pnpm run dev",
    "buildCommand": "pnpm run build",
    "builds": [
        {
            "src": "build/index.js",
            "use": "@vercel/node"
        }
    ]
}